{
  "name": "bash.origin",
  "version": "0.1.23",
  "bin": {
    "bash.origin": "./bash.origin"
  },
  "scripts": {
    "install": "./bash.origin BO install"
  },
  "config": {
    "pio.pinf.io/0": {
      "on": {
        "prepostsync": "./bash.origin BO install -f"
      }
    }
  }
}