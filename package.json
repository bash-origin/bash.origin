{
  "name": "bash.origin",
  "version": "0.1.16",
  "bin": {
    "bash.origin": "./bash.origin"
  },
  "scripts": {
    "install": "./bash.origin BO install"
  },
  "config": {
    "pio.deploy.converter": {
      "name": "nodejs-lib"
    }
  }
}
